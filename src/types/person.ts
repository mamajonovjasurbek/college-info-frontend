export type Person = {
    fish: string
    birthDate: string
    location: string
    passportNumber: string
    phoneNumber: string
    eduDir: string
    course: string
    group: string
    father : string
    mother : string
  }